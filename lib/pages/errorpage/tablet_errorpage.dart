import 'package:flutter/material.dart';

class TabletErrorpage extends StatelessWidget {
  const TabletErrorpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}